# Chatbot API Project

Developed a Laravel 12 chatbot backend that manages multiple bot configurations and exposes REST APIs for chat interactions. Built a secure AI proxy layer that sends frontend requests to an external LLM provider, supports PDF-based instruction uploads, applies request validation and rate limiting, and includes Swagger API documentation plus an admin dashboard for bot management.
